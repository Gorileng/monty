#include "monty.h"
/**
 * f_stack - print top
 * @head: the stack head
 * @counter: the line_number
 * Return: No return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
