#include "monty.h"
/**
  *f_nop- Nothing
  *@head: the stack head
  *@counter: the line_number
  *Return: No return
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
